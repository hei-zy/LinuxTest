#include<stdio.h>
#include<stdlib.h>
#include<string.h>


int main(int argc, char *argv[])
{
    char buf[5];
    printf("sizeof(buf)= %lu, strlen(buf)= %lu\n", sizeof(buf), strlen(buf));



    return 0;
}
